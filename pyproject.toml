[project]
name = "genai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.6.0",
    "diffusers>=0.33.1",
    "safetensors>=0.5.3",
    "scipy>=1.15.2",
    "timm==0.9.12",
    "transformers>=4.51.3",
]
